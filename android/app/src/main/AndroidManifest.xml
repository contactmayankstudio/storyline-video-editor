<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permissions for storage access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Permission for notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Permission for background export (prevent sleep) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permission for haptic feedback on timeline snap -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Require OpenGL ES 3.0 -->
    <uses-feature
        
        android:required="true"
        android:glEsVersion="0x00030000" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@drawable/ic_storyline_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_storyline_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Storyline"
        android:largeHeap="true">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <!-- Main Activity - Basic example -->
        <activity
            android:name="com.video.engine.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Preview Activity - With Timeline Scrubbing (SeekBar) -->
        <activity
            android:name="com.video.engine.PreviewActivity"
            android:exported="true"
            android:label="@string/activity_preview_label"
            android:configChanges="orientation|screenSize|keyboardHidden" />



        <!-- Settings and legal screens -->
        <activity
            android:name="com.video.engine.SettingsActivity"
            android:exported="false"
            android:label="@string/activity_settings_label" />

        <activity
            android:name="com.video.engine.PrivacyPolicyActivity"
            android:exported="false"
            android:label="@string/activity_privacy_label" />

        <activity
            android:name="com.video.engine.TermsActivity"
            android:exported="false"
            android:label="@string/activity_terms_label" />

        <!-- Background Export Service -->
        <service
            android:name="com.video.engine.ExportService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:description="@string/export_service_description" />

        <!-- FileProvider for sharing exported videos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
